services:
  api:
    build: .
    container_name: multi-agent-platform-demo
    environment:
      DATABASE_URL: sqlite:///./data/multi_agent_platform_demo.db
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
